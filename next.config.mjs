/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/CrimsonLetter",
  assetPrefix: "/CrimsonLetter",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
